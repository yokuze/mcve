# Minimal, Complete, Verifiable Examples

<https://stackoverflow.com/help/minimal-reproducible-example>


This repo uses the [`aix` AI config CLI](https://aix.a1st.dev/). To access the
`create-mcve` skill, run `npx -y @a1st/aix install`.
